services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      APP_DATA_DIR: ${APP_DATA_DIR:-/data}
    volumes:
      - app_data:/data

volumes:
  app_data:
